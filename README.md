# 基于 RK3588 的事件驱动无线通信

> SRTP 科研训练项目 · 三端架构：事件端 → GRU 控制端 → WiFi 无线传输

## 一句话概括

本地 RK3588 运行轻量模型（YOLOv8n）做初筛，**GRU 控制端**根据事件时序与信道状态判断"本地能否搞定 / 是否值得传"，仅在**有意义的时刻**通过 WiFi 把任务事件驱动地卸载到远端（PC / 另一块板），从而相比周期性传输大幅降低带宽与延迟。

## 当前选定方案

**组合3：检测 + 姿态估计的级联卸载（跌倒检测 / 养老监护场景）**

- 事件端：`yolov8n` 检测"有没有人"（RK3588 NPU）
- 控制端：GRU 编码检测框时序变化，判断是否疑似异常（如跌倒）
- 卸载：仅疑似异常时把帧发给远端 `yolov8n-pose` 做姿态确认
- 优势：事件极度稀疏（99% 时间不通信），GRU 时序价值最明显，对比效果最悬殊

> 次选方案（组合1：轻量+重量级检测）用于最快跑通 pipeline，详见 `docs/02_技术方案.md`。

## 目录结构

```
RK3588_EventDriven_Comm/
├── README.md                  # 本文件：项目总览
├── docs/                      # 全部规划与参考文档（提问时先看这里）
│   ├── 00_讨论记录摘要.md      # 与 AI 的历史讨论浓缩
│   ├── 01_项目规划.md          # 目标、范围、里程碑
│   ├── 02_技术方案.md          # 三端架构 + 组合1/2/3 细节
│   ├── 03_开发路线图.md        # 分阶段任务与时间估算
│   ├── 04_无板子阶段任务.md    # ★ 当前阶段：手边没板子能做什么
│   └── 05_资源汇总.md          # 模型/数据集/代码仓库链接
├── src/
│   ├── common/                # 事件向量定义、协议、工具函数
│   ├── edge_node/             # 事件端：检测 + 卸载决策（模拟 RK3588）
│   ├── remote_node/           # 远端：姿态估计服务（模拟 PC）
│   ├── controller/            # GRU 控制端：模型定义 + 训练
│   ├── transport/             # WiFi/socket 通信（UDP over localhost）
│   └── simulator/             # 事件流 + 信道仿真器（生成训练数据）
├── experiments/               # 对比实验
│   ├── baselines/             # 周期传输/降采样/固定阈值基线
│   └── results/               # 实验数据与图表输出
├── scripts/                   # ONNX 导出、rknn 转换等一次性脚本
├── models/{pt,onnx,rknn}/     # 模型文件（预训练/转换产物）
├── data/{datasets,recordings}/# 公开数据集与自录视频
├── notebooks/                 # 探索性分析
└── configs/                   # 超参数 / 实验配置
```

## 快速开始（无板子阶段）

见 `docs/04_无板子阶段任务.md`。核心路径：
1. PC 上跑通 `yolov8n` 检测 + `yolov8n-pose` 姿态
2. 用 localhost 双进程模拟"边缘→远端"全链路
3. 下载跌倒数据集，搭对比实验框架
4. 写事件仿真器 + 训练 GRU 控制端

## 环境

- 开发机：Windows + WSL2 (Ubuntu 22.04)
- Python：PyTorch + ultralytics（算法）；rknn-toolkit2（板端转换，仅 Linux）
- 目标板：RK3588（NPU 6TOPS），到手后再介入
