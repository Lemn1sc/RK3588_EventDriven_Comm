# 问题记录（随手记，方便日后提问 / 复盘）

> 遇到卡点、待确认项、想问 AI 的问题都记在这里，标注状态。

## 待确认（阻塞项）

- [ ] 板子系统与 RKNPU 驱动版本？（`cat /proc/rknpu/version`）→ 决定 rknn-toolkit2 版本
- [ ] 摄像头类型：USB 还是 MIPI CSI？分辨率 / 帧率目标？
- [ ] 远端用 PC 还是第二块板？IP / 局域网环境？

## 开放问题（想深入的方向）

- [ ] GRU 相比固定阈值，如何设计实验证明"不可替代性"？
- [ ] 卸载决策的"事后最优标签"怎么定义最合理？
- [ ] 是否要做 INT8 量化？精度损失能否接受？
- [ ] 2026-07-26 单帧高宽比无法区分「真跌倒」与「框被画面边缘截断」——
      `zidane.jpg` 里站立者因下半身出画，h/w=0.91 被判 SUSPECTED（误报）。
      当前结论：单帧几何量的固有上限，交由 T0.6 GRU 用时序区分
      （截断框多帧稳定，跌倒是突变）。待 T0.7 接入真实跌倒数据集后量化这类误报率。
- [ ] 是否扩展多模态（+ yamnet 声音事件）？

## 已解决

- [x] 是否需要自己训练 YOLOv8n？→ 不需要，用 COCO 预训练模型直接检测人。
- [x] Windows 能否跑 rknn-toolkit2？→ 不能，需 WSL2/Linux；但算法部分可直接在 Windows 跑。

---

_模板：`- [ ] YYYY-MM-DD 问题描述 —— 现状 / 结论`_
